package ibm.garage;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetingResourceIT extends InventoryResourceTest {

    // Execute the same tests but in native mode.
}